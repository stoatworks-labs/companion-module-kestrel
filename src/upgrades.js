// No released version has ever changed shape, so there is nothing to upgrade
// from. The array must still exist — `runEntrypoint` requires it — and the
// first real migration goes here.
export const UpgradeScripts = [];
